# ── Backend ────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-jose[cryptography]>=3.3.0
pydantic>=2.7.0
python-multipart>=0.0.9        # required for FastAPI file uploads

# CrewAI + NIM (SOW crew + review crew)
crewai>=0.80.0
openai>=1.30.0
litellm>=1.40.0

# Blueprint visual analysis
PyMuPDF>=1.24.0                # fitz — PDF → PNG conversion for Gemma 3 vision
Pillow>=10.0.0                 # JPEG → PNG conversion fallback

# ── Frontend (Streamlit thin client) ──────────────────────────────────────────
streamlit>=1.35.0
requests>=2.31.0
sseclient-py>=1.8.0

# ── Shared ────────────────────────────────────────────────────────────────────
pandas>=2.0.0
python-dotenv